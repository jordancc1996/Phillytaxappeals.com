---
import type { FaqItem } from "../data/faq";

interface Props {
  items: FaqItem[];
  name?: string;
}

const { items, name = "faq" } = Astro.props;
---
<div class="border-t border-foreground/10" data-faq-accordion>
  {items.map((item) => (
    <details name={name} class="group border-b border-foreground/10">
      <summary class="faq-summary cursor-pointer list-none py-10 font-display text-xl md:text-2xl font-normal text-foreground marker:content-none [&::-webkit-details-marker]:hidden">
        <span class="inline-flex items-start gap-7">
          <span>{item.question}</span>
          <span class="shrink-0 font-display text-xl md:text-2xl leading-none text-foreground group-open:hidden" aria-hidden="true">
            +
          </span>
          <span class="hidden shrink-0 font-display text-xl md:text-2xl leading-none text-foreground group-open:inline" aria-hidden="true">
            −
          </span>
        </span>
      </summary>
      <p class="pt-7 pb-10 font-body text-base md:text-lg leading-relaxed text-foreground">
        {item.answer}
      </p>
    </details>
  ))}
</div>

<style>
  .faq-summary {
    list-style: none;
  }
  .faq-summary:focus {
    outline: none;
  }
  .faq-summary:focus-visible {
    outline: 2px solid hsl(var(--foreground));
    outline-offset: 4px;
  }
</style>
